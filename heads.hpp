#ifndef MINE_HEADS
#define MINE_HEADS

#include <iostream>
#include <vector>
#include <random>
#include <time.h>
#include <map>
#include <queue>
#include <stack>
#include <array>
#include <algorithm> 
#include <conio.h> 
#include <Windows.h>
#include <fstream>

using std::to_string;
using std::swap;
using std::exit;
using std::max;
using std::min;

using std::string;
using std::vector;
using std::pair;
using std::queue;
using std::stack;
using std::ofstream;
using std::ifstream;
using std::ios;
using std::array;

using LL = long long;
using UL = unsigned long long;

#endif
